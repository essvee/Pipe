version: '3'

services:

  backend:
    build:
      context: .
      dockerfile: deploy/backend.dockerfile
    environment:
      DATABASE_URL: mysql://annette:pass@db:3306/annette_db
      DATABASE_HOST: db
      PYTHONUNBUFFERED: 1
    depends_on:
      - db
    volumes:
      - .:/opt/app

  db:
    image: mysql:latest
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: annette_db
      MYSQL_USER: annette
      MYSQL_PASSWORD: pass
      MYSQL_ROOT_PASSWORD: pass
